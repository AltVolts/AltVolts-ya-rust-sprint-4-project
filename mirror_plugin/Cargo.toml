[package]
name = "mirror_plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = {workspace = true}
serde = {workspace = true}

[dev-dependencies]
libloading = "0.8"